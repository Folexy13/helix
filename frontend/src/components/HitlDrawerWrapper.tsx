"use client";

/**
 * HitlDrawerWrapper - DEPRECATED
 * 
 * The side drawer HITL UI has been replaced with inline HITL cards
 * that appear directly in the AgentLogStream conversation flow.
 * 
 * This provides a more natural, chat-like experience where users
 * can respond to agent checkpoints without context switching.
 * 
 * The inline HITL cards are rendered in AgentLogStream.tsx using
 * the HitlInlineCard component.
 */
export default function HitlDrawerWrapper() {
  // No longer rendering the side drawer - HITL is now inline
  return null;
}
